@*@model IEnumerable<AWO_Team14.Models.Vote>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ThumbVote)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ThumbVote)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.VoteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.VoteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.VoteID })
        </td>
    </tr>
}

</table>*@
